

export const sassLoader = {
	loader: 'sass-loader',
	options: {
		// TODO: Add sourcemap support
		// sourceMap: true,
		// TODO: Add this line to have $env variable with value of environment
		// data: "$env: " + process.env.NODE_ENV + ";"
	}
};
